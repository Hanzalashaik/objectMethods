const obj={
    firstname:"hanzala",
    lastName:"shaik",
    age:23,
}

const result=Object.entries(obj);

console.log(result);//[ [ 'firstname', 'hanzala' ], [ 'lastName', 'shaik' ], [ 'age', 23 ] ]

//it is also use to convert object in array